{
	"name": "@hoishin/tslint-config",
	"version": "1.0.0",
	"description": "Sharable TSLint configuration",
	"main": "build/index.js",
	"files": [
		"build"
	],
	"repository": "https://github.com/hoishin/tslint-config",
	"author": "Hoishin",
	"license": "MIT",
	"publishConfig": {
		"access": "public"
	},
	"peerDependencies": {
		"tslint": ">=5.11.0"
	},
	"devDependencies": {
		"del-cli": "^1.1.0",
		"np": "^3.0.4",
		"tslint": "^5.11.0",
		"typescript": "^3.1.1"
	},
	"scripts": {
		"test": "yarn build && (cd test && yarn --check-files && yarn tslint --project .)",
		"build": "del build && tsc --project ."
	}
}
